import React from 'react';
import { Link } from 'react-router-dom';

const Logo = () => (
  <div className="brand-logo">
    <Link to="/">ReactReduxAssignB</Link>
  </div>
);

export default Logo;
